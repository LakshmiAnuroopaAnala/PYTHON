def DecimalToHexadecimal(n):
    val=""
    while n!=0:
        val=val+str(n%16)
        n=n//16
    return val[::-1]
n=int(input("Decimal :"))
print(hex(n))
print("Hexadecimal :",DecimalToHexadecimal(n))
