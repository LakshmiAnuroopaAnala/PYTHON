l=[10,9,11,6,15,2]
print(l)
for i in range(0,len(l)):
    for j in range(0,len(l)-i-1):
        if l[j]>l[j+1]:
            p=l[j]
            l[j]=l[j+1]
            l[j+1]=p
        #print(l)
    #print()   
print(l)
